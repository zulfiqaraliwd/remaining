//QUesstion no 01-----------------------------------------------------------------------------
/*
var arr = ["zulfiqar","abdullah","kazim"]
console.log(arr);
*/

//QUesstion no 02-----------------------------------------------------------------------------

/*
var arr = new Array("zulfiqar","abdullah","kazim");
console.log(arr)
*/

//QUesstion no 03-----------------------------------------------------------------------------

/*var arr = ["zulfiaqr","ali","SMIT","web","developement"];
console.log(arr);
*/

//QUesstion no 04-----------------------------------------------------------------------------

/*
var arr = [1,2,3,4,5,6,7];
console.log(arr);
*/

//QUesstion no 05-----------------------------------------------------------------------------

/*
var arr= [true,false];
console.log(arr)
*/

//QUesstion no 06-----------------------------------------------------------------------------

/*
var arr =["zulfiqar ali",12,true];
console.log(arr);
*/

//QUesstion no 07-----------------------------------------------------------------------------

/*
var arr=["Telenor","Jazz","Ufone","Warid","Zong","Mobilink"];
console.log(arr)
*/

//QUesstion no 08-----------------------------------------------------------------------------

/*
var numbers = [320, 230, 480, 120];

var assending = numbers.sort();

console.log(numbers);
*/

//QUesstion no 09---------------------------------------------------------------------------
    /*
    var arr = ["Avengers:age of ujrons","specture","jurassis world","inside out"]
console.log("length of the array " + arr.length());
*/

//QUesstion no 10-----------------------------------------------------------------------------

/*
var arr = ["Audi","Volvo","Ford","Lanbarghoni"];
console.log(arr.indexOf("Audi"));
console.log(arr.indexOf("Lanbarghoni"))
*/

//QUesstion no 11-----------------------------------------------------------------------------

/*
var name = ["Michel","John","Tony"];
var marks = ["64%","46%","96%"];

console.log(name.)
*/

//QUesstion no 12-----------------------------------------------------------------------------


/*
        
        var colors = ["Red", "Blue", "Green", "Yellow"];
        console.log("<p>Initial Array: " + colors + "</p>");

        
        var Beginning = prompt("Enter a color to add to the beginning of the array:");
        if (Beginning) {
            colors.unshift(Beginning);
        }
        console.log("<p>After adding a color to the beginning: " + colors + "</p>");

        var End = prompt("Enter a color to add to the end of the array:");
        if (End) {
            colors.push(End);
        }
        console.log("<p>After adding a color to the end: " + colors + "</p>");

        colors.unshift("Pink", "Orange");
        console.log("<p>After adding two colors to the beginning: " + colors + "</p>");




        colors.shift();
        console.log("<p>After deleting the first color: " + colors + "</p>");

        
        colors.pop();
        console.log("<p>After deleting the last color: " + colors + "</p>");

        var Add = parseInt(prompt("At which index do you want to add a color?"));
        var Add = prompt("Enter the color to add at index " + Add);
        if (Add && !isNaN(Add)) {
            colors.splice(Add, 0, Add);
        }
        console.log("<p>After adding a color at index " + Add + ": " + colors + "</p>");

        
        var Delete = parseInt(prompt("At which index do you want to delete color(s)?"));
        var toDelete = parseInt(prompt("How many colors do you want to delete?"));
        if (!isNaN(toDelete) && !isNaN(toDelete)) {
        colors.splice(Delete, toDelete); 
        }
        console.log("<p>After deleting " + toDelete + " color(s) from index " + Delete + ": " + colors + "</p>");

    */



//QUesstion no 13-----------------------------------------------------------------------------

//QUesstion no 14-----------------------------------------------------------------------------

/*
var fruit = ["strawberry","apple","orange","banana"];

console.log(fruit.sort());
*/

//QUesstion no 15-----------------------------------------------------------------------------

/*
var cities = ["karachi","lahore","islamabad","quetta"];
var newCities=cities.slice(1,3);
console.log(newCities);
*/

//QUesstion no 16-----------------------------------------------------------------------------

/*
var animal = ["this","is","my","cat"];
var newAnimal= animal.join(',');
console.log(newAnimal);
*/


//QUesstion no 17-----------------------------------------------------------------------------

/*
var devices= ["Keyboard", "Mouse", "Printer"];

devices.push("Monitor"); 
console.log(devices.shift());

console.log(devices);       
*/

//QUesstion no 18-----------------------------------------------------------------------------


/*
var devices= ["Keyboard", "Mouse", "Printer"];

devices.pop("Monitor"); 
console.log(devices.shift());

console.log(devices);       
*/


//QUesstion no 19-----------------------------------------------------------------------------

/*multi ddimentiional array------------*/

//QUesstion no 20-----------------------------------------------------------------------------